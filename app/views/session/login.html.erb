<% flash.each do |_type, msg| %>
  <p style="color: red"><%= msg %></p>
<% end %>

<%= form_with url: session_create_url, method: :post, local: true do |f| %>
  <%= f.text_field :login, required: true, label: 'Логин' %>
  <%= f.password_field :password, required: true, label: 'Пароль' %>
  <%= f.submit 'Войти' %>
<% end %>
<%= link_to 'Зарегистрироваться', new_user_path %>