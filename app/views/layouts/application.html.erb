<!DOCTYPE html>
<html>
  <head>
    <title>Lab12Ipl</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if signed_in? %>
      <p><%= link_to "Выйти", session_logout_path %></p>
    <% end %>
    <%= yield %>
  </body>
</html>
